# 🚀 دليل تشغيل NOS Knowledge Base

موقع KB مستقل: **قراءة للكل بدون تسجيل دخول**، والتعديل **برقم سري واحد** محمي على مستوى قاعدة البيانات.

---

## 1) شغّل الـ SQL على Supabase

- افتح مشروع Supabase بتاعك (نفس مشروع البورتالات — الجدول ده منفصل ومش بيلمس بياناتهم).
- **SQL Editor → New query** → الصق كل محتوى `migrations/001_articles_schema.sql`.
- **⚠️ قبل ما تدوس Run:** غيّر السطر ده لرقمك السري:
  ```
  values (1, crypt('change-me-please', gen_salt('bf')))
  ```
  بدّل `change-me-please` بالرقم/الجملة السرية اللي إنت تختارها.
- دوس **Run**.

ده بيعمل: جدول `articles` + جدول إعدادات للرقم السري + قراءة عامة + 3 functions محمية للكتابة + 4 مقالات تجريبية.

---

## 2) بيانات الاتصال

انسخ `.env.local.example` لـ `.env.local` وحط قيمك:
```
NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGci...
```
تلاقيهم في: **Supabase → Settings → API**.
> استخدم مفتاح **anon** بس. متحطّش الـ service_role أبداً.

---

## 3) شغّل محلياً أو انشر
```
npm install
npm run dev      # للتجربة
```
النشر: Vercel (اربط الريبو + حط الـ env vars) أو أي استضافة Next.js.

---

## إزاي التعديل بيشتغل؟

- أي زائر → يفتح الموقع ويقرأ. مفيش login.
- الأدمن → يدوس **Edit** فوق → يكتب الرقم السري → تظهر أزرار الإضافة/التعديل/الحذف.
- الرقم مش بيتحفظ في المتصفح — بيتقفل تلقائي لما تعمل Logout أو تقفل التاب.

## الأمان الحقيقي (ليه ده مختلف عن admin123)

- الكتابة **ممنوعة تماماً** بالـ anon key المباشر (RLS مفيهاش أي policy كتابة).
- التعديل بيعدّي على functions في الداتابيز بتتأكد من الرقم السري (bcrypt) قبل ما تكتب.
- يعني حتى لو حد فتح DevTools واتخطى الواجهة → الداتابيز نفسها هترفض من غير الرقم الصح.

## تغيير الرقم السري لاحقاً
شغّل السطر ده في SQL Editor برقمك الجديد:
```sql
update public.kb_settings
set edit_key_hash = crypt('رقمك-الجديد', gen_salt('bf'))
where id = 1;
```
